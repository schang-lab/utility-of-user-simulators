# UserLM Data Preparation Pipeline Requirements

# Core dependencies
# torch pinned to a CUDA 12.8 build due to our compute-specific constraints
# you can adjust the version as needed
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchvision==0.25.0+cu128
transformers>=4.36.0
datasets>=2.14.0

# API and utilities
openai>=1.0.0
tqdm>=4.65.0
wandb>=0.19.6

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Optional: for faster processing
# Pinned: accelerate>=1.14 renamed is_ccl_available -> is_xccl_available
# which breaks llama-cookbook's train_utils.py import
# 1.12.0 matches the working env
accelerate==1.12.0
